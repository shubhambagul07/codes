#include <xc.h>
#define _XTAL_FREQ 20000000

void main(void) {
    unsigned char a = 12, b = 3, result1, result2;

    result1 = a * b;  // Multiplication
    result2 = a / b;  // Division

    while(1);
}
