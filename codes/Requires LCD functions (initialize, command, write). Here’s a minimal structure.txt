#include <xc.h>
#define _XTAL_FREQ 20000000

void lcd_init();
void lcd_cmd(unsigned char cmd);
void lcd_data(unsigned char data);

void main(void) {
    TRISD = 0x00;
    lcd_init();
    lcd_cmd(0x80);      // Go to line 1
    lcd_data('H');
    lcd_data('i');
    while(1);
}

// Implement lcd_init(), lcd_cmd(), lcd_data() based on your LCD connection
